```jsx
// Log message added to show the job details
console.log(jobDetails);
```

```jsx
// Log message added to show the job ID before deletion
console.log(job._id);
```

```jsx
// Log message added to show the error response in case of job deletion failure
console.log(err.response);
```

```jsx
// Log message added to show the error response in case of job update failure
console.log(err.response);
```

```jsx
// Log message added to show the query string for fetching jobs
console.log(queryString);
```

```jsx
// Log message added to show the address for fetching jobs
console.log(address);
```

```jsx
// Log message added to show the response data after fetching jobs
console.log(response.data);
```

```jsx
// Log message added to show the error response in case of fetching jobs failure
console.log(err.response.data);
```