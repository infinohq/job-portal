```jsx
// Log file when a file is selected
console.log(event.target.files);

// Log the selected file before uploading
console.log(file);

// Log the response data after successful upload
console.log(response.data);

// Log the error response if upload fails
console.log(err.response);
```